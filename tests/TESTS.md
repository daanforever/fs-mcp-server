# Тесты MCP File Edit Server

## Пограничные случаи (Edge Cases)

Все тесты были проверены и подтверждены работают корректно.

### 1. Пустой файл (content='')
```bash
# Создание файла с пустым содержимым
echo '{"method":"edit_file","arguments":{"filename":"empty.txt","content":""}}' | ./mcp-file-edit
# ✓ Создает файл 0 байт
```

### 2. Замена текста (old_text/new_text)
```bash
# Замена "old" на "new"
echo '{"method":"edit_file","arguments":{"filename":"file.txt","content":"old value"}}' | ./mcp-file-edit
echo '{"method":"edit_file","arguments":{"filename":"file.txt","old_text":"old","new_text":"new"}}' | ./mcp-file-edit
# ✓ Результат: "new value"
```

### 3. Удаление текста (old_text без new_text)
```bash
# Удаление строки
echo '{"method":"edit_file","arguments":{"filename":"file.txt","content":"remove me\nkeep"}}' | ./mcp-file-edit
echo '{"method":"edit_file","arguments":{"filename":"file.txt","old_text":"remove me\n"}}' | ./mcp-file-edit
# ✓ Результат: "keep"
```

### 4. Множественные вхождения
```bash
# Замена всех вхождений
echo '{"method":"edit_file","arguments":{"filename":"file.txt","content":"old old old"}}' | ./mcp-file-edit
echo '{"method":"edit_file","arguments":{"filename":"file.txt","old_text":"old","new_text":"NEW"}}' | ./mcp-file-edit
# ✓ Результат: "NEW NEW NEW"
```

### 5. Вложенные директории
```bash
# Автоматическое создание путей
echo '{"method":"edit_file","arguments":{"filename":"a/b/c/file.txt","content":"content"}}' | ./mcp-file-edit
# ✓ Создает a/b/c/ и файл
```

### 6. Чтение несуществующего файла
```bash
# Возвращает ошибку
echo '{"method":"read_file","arguments":{"filename":"missing.txt"}}' | ./mcp-file-edit
# ✓ {"error":{"code":-32000,"message":"Failed to read file: ..."}}
```

### 7. Полная замена через *
```bash
# Замена всего содержимого
echo '{"method":"edit_file","arguments":{"filename":"file.txt","content":"old"}}' | ./mcp-file-edit
echo '{"method":"edit_file","arguments":{"filename":"file.txt","old_text":"*","new_text":"NEW"}}' | ./mcp-file-edit
# ✓ Результат: "NEW"
```

### 8. UTF-8 символы
```bash
# Поддержка Unicode
echo '{"method":"edit_file","arguments":{"filename":"utf8.txt","content":"Привет 🌍"}}' | ./mcp-file-edit
# ✓ ✓ Сохраняет корректно
```

### 9. Добавление в конец (new_text)
```bash
# Без old_text добавляет в конец
echo '{"method":"edit_file","arguments":{"filename":"file.txt","content":"start"}}' | ./mcp-file-edit
echo '{"method":"edit_file","arguments":{"filename":"file.txt","new_text":"end"}}' | ./mcp-file-edit
# ✓ Результат: "start\nend"
```

### 10. Content приоритетнее
```bash
# Параметр content перекрывает old_text/new_text
echo '{"method":"edit_file","arguments":{"filename":"file.txt","content":"second","old_text":"ignored"}}' | ./mcp-file-edit
# ✓ Записывает "second", игнорируя old_text
```

### 11. Замена несуществующего текста
```bash
# Если old_text не найден, new_text добавляется в конец
echo '{"method":"edit_file","arguments":{"filename":"file.txt","content":"base"}}' | ./mcp-file-edit
echo '{"method":"edit_file","arguments":{"filename":"file.txt","old_text":"missing","new_text":"added"}}' | ./mcp-file-edit
# ✓ Результат: "base\nadded"
```

### 12. Файлы без расширения
```bash
echo '{"method":"edit_file","arguments":{"filename":"noext","content":"test"}}' | ./mcp-file-edit
# ✓ Работает корректно
```

### 13. Перезапись существующего файла
```bash
echo '{"method":"edit_file","arguments":{"filename":"file.txt","content":"OLD"}}' | ./mcp-file-edit
echo '{"method":"edit_file","arguments":{"filename":"file.txt","content":"NEW"}}' | ./mcp-file-edit
# ✓ Полностью заменяет содержимое
```

### 14. Замена в пустом файле
```bash
echo '{"method":"edit_file","arguments":{"filename":"empty.txt","content":""}}' | ./mcp-file-edit
echo '{"method":"edit_file","arguments":{"filename":"empty.txt","old_text":"","new_text":"text"}}' | ./mcp-file-edit
# ✓ Добавляет текст в пустой файл
```

### 15. Многолиновый новый текст
```bash
echo '{"method":"edit_file","arguments":{"filename":"file.txt","content":"start"}}' | ./mcp-file-edit
echo '{"method":"edit_file","arguments":{"filename":"file.txt","old_text":"start","new_text":"line1\nline2\nline3"}}' | ./mcp-file-edit
# ✓ Поддерживает переносы строк в new_text
```

## Тесты read_file

Функция `read_file` поддерживает два формата передачи аргументов для совместимости с различными MCP клиентами.

### Формат 1: arguments как объект (стандартный)
```bash
echo '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"read_file","arguments":{"filename":"file.txt"}}}' | ./mcp-file-edit
```

### Формат 2: arguments как строка JSON (для совместимости)
```bash
echo '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"read_file","arguments":"{\"filename\":\"file.txt\"}"}}' | ./mcp-file-edit
```

### Тестовые случаи

#### 1. Чтение обычного файла
- ✓ Оба формата возвращают одинаковый результат
- ✓ Результат содержит поле `content` со содержимым файла

#### 2. Чтение многострочного файла
- ✓ Корректно читает файлы с несколькими строками
- ✓ Сохраняет все переносы строк

#### 3. Чтение пустого файла
- ✓ Возвращает пустую строку в поле `content`
- ✓ Не возвращает ошибку для пустых файлов

#### 4. Чтение файла с UTF-8
- ✓ Корректно обрабатывает Unicode символы
- ✓ Сохраняет эмодзи и специальные символы

#### 5. Формат ответа
- ✓ Возвращает объект с полем `content`
- ✓ Не содержит поле `status` (упрощенный формат)

#### 6. Обработка ошибок
- ✓ Несуществующий файл возвращает ошибку
- ✓ Отсутствие параметра `filename` возвращает ошибку
- ✓ Некорректный JSON в `arguments` возвращает ошибку

#### 7. Совместимость форматов
- ✓ Оба формата (объект и строка) работают идентично
- ✓ Результаты полностью совпадают

#### 8. Вложенные директории
- ✓ Корректно читает файлы из вложенных директорий
- ✓ Работает с обоими форматами аргументов

### Запуск тестов

```bash
./tests/test_read_file.sh
```

Тесты проверяют:
- 17 тестовых случаев
- Оба формата передачи аргументов
- Обработку ошибок
- Совместимость форматов

## Тесты exec tool

Функция `exec` выполняет shell команды с поддержкой таймаутов, рабочих директорий и захвата stdout/stderr.

### Параметры
- `command` (string, required): Команда для выполнения
- `timeout` (integer, optional): Таймаут в секундах (по умолчанию: 300)
- `work_dir` (string, optional): Рабочая директория (по умолчанию: текущая директория)

### Формат ответа
```json
{
  "stdout": "вывод команды",
  "stderr": "ошибки команды",
  "exit_code": 0,
  "status": "success" | "failed",
  "timeout": false
}
```

### Тестовые случаи

#### 1. Базовые тесты выполнения команд
- ✓ Простая команда (echo)
- ✓ Вывод в stdout
- ✓ Вывод в stderr
- ✓ Вывод в оба потока (stdout и stderr)
- ✓ Exit code успешной команды (0)
- ✓ Exit code неуспешной команды (не 0)
- ✓ Статус успешной команды ("success")
- ✓ Статус неуспешной команды ("failed")

#### 2. Тесты рабочей директории (work_dir)
- ✓ Выполнение команды в указанной директории
- ✓ Выполнение команды без указания work_dir (текущая директория)
- ✓ Создание файла в указанной директории
- ✓ Ошибка: несуществующая директория
- ✓ Ошибка: work_dir не является директорией (файл)

#### 3. Тесты таймаутов
- ✓ Команда завершается до таймаута
- ✓ Команда превышает таймаут (возвращает ошибку)
- ✓ Таймаут по умолчанию (300 секунд) для быстрой команды
- ✓ Очень короткий таймаут (команда успевает)
- ✓ Отрицательный таймаут (обработка)
- ✓ Нулевой таймаут (обработка)
- ✓ Очень большой таймаут

#### 4. Тесты обработки ошибок
- ✓ Команда не найдена (возвращает ошибку)
- ✓ Отсутствие обязательного параметра command
- ✓ Некорректный JSON в arguments
- ✓ Команда с синтаксической ошибкой (возвращает exit_code != 0)

#### 5. Тесты пограничных случаев (edge cases)
- ✓ Пустая команда
- ✓ Команда с пробелами
- ✓ Команда с переносами строк
- ✓ Команда со специальными символами
- ✓ Команда с UTF-8 символами
- ✓ Длинная команда
- ✓ Команда с переменными окружения
- ✓ Команда с перенаправлением вывода
- ✓ Команда с pipe (|)
- ✓ Команда с несколькими командами (&&)
- ✓ Команда с несколькими командами (||)
- ✓ Exit code > 1
- ✓ Отрицательный таймаут
- ✓ Нулевой таймаут
- ✓ Очень большой таймаут

#### 6. Тесты формата ответа
- ✓ Наличие всех полей в ответе (stdout, stderr, exit_code, status, timeout)
- ✓ Типы полей в ответе (строки, числа, булевы значения)

### Запуск тестов

```bash
./tests/test_exec.sh
```

Тесты проверяют:
- 50+ тестовых случаев
- Базовое выполнение команд
- Рабочие директории
- Таймауты
- Обработку ошибок
- Пограничные случаи
- Формат ответа

## Итоги

Все 15 тестов пограничных случаев для `edit_file` пройдены успешно! ✓
Все 17 тестов для `read_file` пройдены успешно! ✓
Все 50+ тестов для `exec` пройдены успешно! ✓

Сервер корректно обрабатывает:
- Пустые файлы и строки
- Спецсимволы и UTF-8
- Вложенные директории
- Отсутствующие файлы
- Множественные вхождения
- Различные режимы редактирования
- Два формата передачи аргументов для `read_file` (объект и строка JSON)
- Выполнение shell команд с таймаутами
- Рабочие директории для команд
- Захват stdout и stderr отдельно
- Обработку exit codes и статусов
